{
  "name": "jev-tetris",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tetris played by Jev, TypeSafe's System One model.",
  "scripts": {
    "start": "node server.js",
    "start:env": "node --env-file=.env server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@typesafe-ai/sdk": "^0.6.0"
  }
}
